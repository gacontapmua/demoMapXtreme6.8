Imports MapInfo.Mapping

Public Class UserDrawLines
    Inherits UserDrawLayer

    Public Overrides Sub Draw(clientRect As Rectangle, updateRect As Rectangle, graphics As Graphics)

        'Throw New NotImplementedException()
    End Sub

End Class
